package gui.login;

public class LoginNegativePathsTest {

    //1. Login with already registered user - NOT valid credentials
    //1.1. Login with already registered user - NOT valid password
    //1.2. Login with already registered user - NOT valid user

    //2. Login with newly created/registered user -not  valid credentials
    //2.1. Login with newly created/registered user  - NOT valid password
    //2.1. Login with newly created/registered user  - NOT valid user


}
